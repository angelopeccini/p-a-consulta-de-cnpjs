# P&A Consulta de CNPJs

Backend para Render com consulta PJe/TRF1 por CNPJ e base aberta da PGFN.

Implante como Blueprint usando `render.yaml`. Depois configure `PGFN_DATA_URLS` com os links oficiais e execute o cron manualmente na primeira carga.
